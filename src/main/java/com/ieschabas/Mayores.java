package com.ieschabas;

/**
 *Programa que escribe un algoritmo que devuelva el mayor de dos números.
 *El usuario introduce dos número y muestra por pantalla el mayor.
 * @author Alejandro
 * @version 1.0
 */
public class Mayores {
    // Método que devuelve el mayor de dos números
    public int max2(int a, int b) {
        if (a > b) {
            return a;
        } else {
            return b;
        }
    }
}
